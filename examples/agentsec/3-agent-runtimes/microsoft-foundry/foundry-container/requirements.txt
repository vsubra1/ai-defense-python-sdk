# Foundry Container Agent Dependencies
# These are installed in the Docker container

# Azure ML inference server - provides HTTP server for main.py init()/run()
# NOTE: This includes Flask and gunicorn as dependencies - do NOT specify them separately
# to avoid version conflicts (per Azure ML documentation)
azureml-inference-server-http>=1.0.0

# LangChain for agent orchestration
langchain-openai>=0.1.0
langchain-core>=0.2.0

# MCP client for tool integration
mcp>=1.11.0

# Environment variable loading
python-dotenv>=1.0.0

# HTTP client (used by aidefense)
httpx>=0.24.0
aiohttp>=3.9.0
requests>=2.32.0

# Nested async support (for MCP tools)
nest-asyncio>=1.5.0

# Required by aidefense SDK
wrapt>=1.15.0
pydantic>=2.0.0
pyyaml>=6.0
uuid>=1.30

# Note: aidefense SDK is copied from source (not yet on PyPI)
