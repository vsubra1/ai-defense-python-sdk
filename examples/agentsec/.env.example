# =============================================================================
# Environment Variables for agentsec Examples
# =============================================================================
# Secrets, credentials, and environment-specific settings.
# Non-secret configuration (integration modes, gateway URLs, endpoints,
# timeouts, inspection modes) lives in agentsec.yaml.
#
# agentsec.yaml references these values via ${VAR_NAME} syntax.

# -- Integration Mode Override (used by example scripts) ---------------------
# Override the llm_integration_mode / mcp_integration_mode set in agentsec.yaml
# at runtime without editing the YAML file.  Example scripts read these and
# pass the value to protect().  Uncomment to override.
# AGENTSEC_LLM_INTEGRATION_MODE=api      # api | gateway
# AGENTSEC_MCP_INTEGRATION_MODE=api      # api | gateway

# -- AI Defense API Mode Keys ------------------------------------------------
AI_DEFENSE_API_MODE_LLM_API_KEY=your-ai-defense-llm-api-key
AI_DEFENSE_API_MODE_MCP_API_KEY=your-ai-defense-mcp-api-key

# -- LLM Provider API Keys --------------------------------------------------
# The same key is used for both API mode (direct calls) and gateway mode
# (referenced via ${VAR_NAME} in agentsec.yaml).

# OpenAI
OPENAI_API_KEY=sk-your-openai-api-key
OPENAI_1_GATEWAY_URL=https://your-openai-gateway-url

# Azure OpenAI
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_KEY=your-azure-openai-key
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4o
AZURE_OPENAI_API_VERSION=2024-08-01-preview
AZURE_OPENAI_1_GATEWAY_URL=https://your-azure-openai-gateway-url

# Cohere
COHERE_API_KEY=
COHERE_1_GATEWAY_URL=https://your-cohere-gateway-url

# Mistral AI
MISTRAL_API_KEY=
MISTRAL_1_GATEWAY_URL=https://your-mistral-gateway-url

# -- AWS Bedrock (Global -- deploy scripts, examples, framework config) ------
# These global vars are used by deploy scripts, boto3 client creation in
# examples, and agent-framework config-bedrock.yaml templates.
AWS_AUTH_METHOD=profile
AWS_REGION=us-east-1
AWS_PROFILE=default

# -- AWS Bedrock Per-Gateway (optional -- gateway mode SigV4 only) -----------
# Per-gateway AWS credentials for bedrock-1 and bedrock-2 in agentsec.yaml.
# Referenced via ${VAR} substitution. When not set, falls back to default
# boto3 credential chain (env vars, ~/.aws/credentials, instance role).
#
# Style 1: Profile-based (recommended)
BEDROCK_1_AWS_REGION=us-east-1
BEDROCK_1_AWS_PROFILE=default
BEDROCK_1_GATEWAY_URL=https://your-bedrock-1-gateway-url
BEDROCK_2_AWS_REGION=eu-west-1
BEDROCK_2_AWS_PROFILE=team-b
BEDROCK_2_GATEWAY_URL=https://your-bedrock-2-gateway-url
#
# Style 2: Explicit keys (uncomment if using static/temporary credentials)
# BEDROCK_1_AWS_ACCESS_KEY_ID=
# BEDROCK_1_AWS_SECRET_ACCESS_KEY=
# BEDROCK_1_AWS_SESSION_TOKEN=
# BEDROCK_2_AWS_ACCESS_KEY_ID=
# BEDROCK_2_AWS_SECRET_ACCESS_KEY=
# BEDROCK_2_AWS_SESSION_TOKEN=

# -- GCP Vertex AI (Global -- deploy scripts, examples, framework config) -----
# Used for authenticating to Vertex AI for LLM calls and deploying to
# Agent Engine / Cloud Run / GKE. No API key needed for gateway mode (uses ADC).
# Authenticate via: gcloud auth application-default login
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GOOGLE_CLOUD_LOCATION=us-central1
GOOGLE_AI_SDK=vertexai
# GKE deployment only:
GKE_AUTHORIZED_NETWORKS=YOUR_PUBLIC_IP/32

# -- GCP Vertex AI Per-Gateway (optional -- gateway mode ADC only) -----------
# Per-gateway GCP credentials for vertexai-1 in agentsec.yaml.
# Referenced via ${VAR} substitution. When not set, falls back to default
# google.auth.default() (env vars, gcloud ADC, instance metadata).
#
# Style 1: Project/Location (recommended)
VERTEXAI_1_GCP_PROJECT=your-gcp-project-id
VERTEXAI_1_GCP_LOCATION=us-central1
VERTEXAI_1_GATEWAY_URL=https://your-vertexai-gateway-url
#
# Style 2: Explicit service account key file (uncomment)
# VERTEXAI_1_SA_KEY_FILE=/path/to/service-account.json
#
# Style 3: Service account impersonation (uncomment)
# VERTEXAI_1_TARGET_SA=my-sa@project.iam.gserviceaccount.com

# -- Azure AI Foundry (Deployment) -------------------------------------------
# Used for deploying to Azure AI Foundry (agent-app, functions, container).
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
AZURE_SUBSCRIPTION_ID=
AZURE_RESOURCE_GROUP=
AZURE_AI_FOUNDRY_PROJECT=
AZURE_AI_FOUNDRY_ENDPOINT=
AZURE_ACR_NAME=
AZURE_ACR_LOGIN_SERVER=
AZURE_FUNCTION_APP_NAME=
AZURE_STORAGE_ACCOUNT=

# -- MCP Server URL ----------------------------------------------------------
MCP_SERVER_URL=https://remote.mcpservers.org/fetch/mcp
MCP_FETCH_GATEWAY_URL=https://your-mcp-fetch-gateway-url
MCP_TIME_GATEWAY_URL=https://your-mcp-time-gateway-url

# -- AI Defense API Mode Endpoints -------------------------------------------
AI_DEFENSE_API_MODE_LLM_ENDPOINT=https://your-ai-defense-llm-api-endpoint
AI_DEFENSE_API_MODE_MCP_ENDPOINT=https://your-ai-defense-mcp-api-endpoint

# -- MCP Gateway Auth (per-server, optional) ---------------------------------
# Used by agentsec.yaml mcp_gateways entries that need authentication.
# Uncomment and fill in the values for your auth mode.
#
# API Key auth (auth_mode: api_key)
# MCP_GATEWAY_API_KEY=your-mcp-gateway-api-key
#
# OAuth 2.0 Client Credentials (auth_mode: oauth2_client_credentials)
# MCP_OAUTH_CLIENT_ID=your-oauth-client-id
# MCP_OAUTH_CLIENT_SECRET=your-oauth-client-secret
